# ── Credentials & local config ─────────────────────────────────────────────
config.json

# ── Secrets / credentials that should never be committed ───────────────────
*.key
*.pem
*.env
.env*

# ── Template JSONs (contain environment-specific blob storage shapes) ───────
blob_dest_template*.json
route_template*.json

# ── Runtime output ──────────────────────────────────────────────────────────
cribl_snapshots/
ops_rm_r_templates_output/
*.log

# ── Bulk app input files (may contain internal app IDs) ────────────────────
appids.txt

# ── Python ──────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/
pip-wheel-metadata/
*.whl

# ── Virtual environments ─────────────────────────────────────────────────────
.venv/
venv/
env/
ENV/

# ── Docker artifacts ────────────────────────────────────────────────────────
cribl-pusher.tar
cribl-pusher.part.*
cribl-pusher.tar.sha256

# ── Claude Code ─────────────────────────────────────────────────────────────
.claude/

# ── OS / editor ─────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
*.swp
*.swo
*~
